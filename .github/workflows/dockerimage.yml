name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build --platform linux/amd64,linux/arm64,linux/arm/v7 . --file Dockerfile --tag ampache:latest
